services:
  - type: web
    name: image-to-song-final
    env: python
    repo: https://github.com/Mokshitha-nelluri/image-to-song.git
    rootDir: .
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn main_complete_pipeline:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: SPOTIFY_CLIENT_ID
        sync: false
      - key: SPOTIFY_CLIENT_SECRET
        sync: false
      - key: SPOTIFY_REDIRECT_URI
        sync: false
