name: mobile_app
description: "🎵 Image-to-Song Mobile App - Turn your photos into personalized music!"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # HTTP client for API calls
  http: ^1.1.0
  
  # Image picker for camera/gallery
  image_picker: ^1.0.4
  
  # URL launcher for Spotify links
  url_launcher: ^6.2.1
  
  # In-app WebView for OAuth
  webview_flutter: ^4.4.2
  
  # JSON handling
  json_annotation: ^4.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
